package com.project_07.Auth_User_service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class AuthUserServiceApplication {

	public static void main(String[] args) {
		SpringApplication.run(AuthUserServiceApplication.class, args);
	}

}
// This is the main entry point for the Auth_User_service application.
// It uses Spring Boot to bootstrap the application and start the embedded server.